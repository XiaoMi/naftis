mode="debug"
istioctl="/user/bin/istioctl"
[logger]
    [logger.console]
        color=true
        prefix="[naftis]"
        level="Debug"
    [logger.zap]
        path="naftis-api.log"
        level="Debug"
[db]
    default="user:password@tcp(host:port)/naftis?charset=utf8&parseTime=True&loc=Local" # TODO replace MySQL DSN with your own